// Default exports can be imported using any identifier name of your choice
import userName from './script.js'

// Named exports must be imported using their exact identifier name (inside curly braces)
import { arr } from './script.js'

console.log(userName);
console.log(arr);
